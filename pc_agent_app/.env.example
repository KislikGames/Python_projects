# Required for LLM planning/execution
OPENAI_API_KEY=your_openai_api_key_here

# Optional model override
OPENAI_MODEL=gpt-4.1-mini

# Optional sandbox for local testing (when enabled, file tool writes into this folder)
AGENT_SANDBOX_DIR=
